import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const Post = ({post}) => {
    return (
        <View>

        </View>
    );
};

export default Post;

const styles = StyleSheet.create({

})
